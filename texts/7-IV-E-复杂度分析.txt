首先,我们研究PnP所需要的消息交换次数。网络中的节点数用n=|n |表示，选取m个节点作为决策委员会。由于排序过程不需要节点之间交互，因此只有排序结果传播才会产生O（mn）消息。然后，还需要O（mn）的消息去按节点报告支付需求。PBFT协议引起决策委员会成员之间的O（m2）的消息交换。最后，通过计算传播规划结果的O（mn）消息，PnP的通信复杂度为O（mn+m2）。快速多项式时间算法存在于可验证随机函数和线性规划问题uniBP线性和biBP线性。考虑到区块链节点通常具有很强的计算能力，所以PnP对它们的计算开销较小。